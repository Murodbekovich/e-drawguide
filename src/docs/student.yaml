tags:
  name: Student
  description: Talabalar uchun kontentlar

paths:
  /student/library:
    get:
      summary: Kitoblar ro'yxati
      tags: [Student]
      security:
        - BearerAuth: []
      parameters:
        - in: query
          name: search
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
      responses:
        200:
          description: Kitoblar ro'yxati
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Book'