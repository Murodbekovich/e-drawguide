components:
  schemas:
    User:
      type: object
      properties:
        id: { type: string, format: uuid }
        full_name: { type: string }
    Video:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
    Library:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
    Quiz:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
    Result:
      type: object
      properties:
        result_id: { type: string, format: uuid }
        score: { type: integer }